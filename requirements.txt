# Helios Voice + Vision Server (Pi-side)
# Install: pip install -r requirements.txt

# APIs
anthropic>=0.40.0
httpx>=0.24.0
websockets>=11.0

# Audio
opuslib>=3.0.1
numpy>=1.24.0

# Config
python-dotenv>=1.0.0

# BLE (for OTA rescue push to pendant via scripts/ble_ota.py only)
bleak>=0.21.0
